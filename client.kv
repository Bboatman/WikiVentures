#:kivy 1.9.1


<Body>:
	size: 50, 50
	canvas:
		Ellipse:
			pos: (self.x, self.y)
			size: (self.size, self.size)
			source: self.source

<Spaceship>:
	size: 128, 128
	canvas:
		Rectangle:
			size: (self.size, self.size)
			source: self.source
			pos: (self.x, self.y)
